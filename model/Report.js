const mongoose = require("mongoose");

const reportSchema = new mongoose.Schema({
  plateNumber: {
    type: String,
    required: true,
  },
  location: {
    type: String,
    required: true,
  },
  description: {
    type: String,
    required: true,
  },
});
